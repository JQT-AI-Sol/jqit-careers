// Empty module used to alias `server-only` in the test environment.
// `server-only` throws when imported outside a React Server Component build,
// so we replace it with a no-op for unit/API tests.
export {};
